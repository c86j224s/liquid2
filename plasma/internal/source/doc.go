// Package source owns approved source snapshot records, locators, observation
// state, and consumer-side persistence ports. Connector clients, fetch policy,
// SQL shape, and transport messages belong to adapters or source services.
package source
