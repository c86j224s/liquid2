// Package source owns approved source snapshot records, locators, observation
// state, and consumer-side persistence ports. Connector clients, fetch policy,
// SQL shape, filesystem/configuration adapters, and transport messages belong to adapters or source services.
// The LocalPathReader port and its observation DTO closure are owned here.
package source
