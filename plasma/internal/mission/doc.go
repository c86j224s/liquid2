// Package mission owns mission identity, lifecycle-facing read models, active
// work summaries, and their consumer-side persistence ports. It does not own
// transport payloads, SQL shape, agent execution, or report/workflow policy.
package mission
