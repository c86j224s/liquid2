// Package ledger owns Plasma's durable event record and the consumer-side
// ports used to append and read that record. It does not decide feature event
// names, payload policy, storage schema, or transport representation.
package ledger
