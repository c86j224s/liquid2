// Package mcpdocs owns the code-owned static Markdown documents exposed through
// Plasma's MCP resources surface.
//
// The package is intentionally static: it embeds only the five English
// canonical runtime copies and must not read mission state, source bodies,
// session identifiers, ledger contents, runtime configuration, Korean
// counterparts, README files, or provider responses.
package mcpdocs
