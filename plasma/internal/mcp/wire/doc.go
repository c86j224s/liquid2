// Package wire owns the small MCP JSON envelope shared by transport composition
// and focused MCP tool families. It deliberately contains no dispatch,
// idempotency, binding, or product workflow policy.
package wire
