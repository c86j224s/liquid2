# MCP Random-seek Surface Experiment

Primary purpose: test whether a bounded mission-scoped random-seek surface helps
source navigation and final report quality.

All cells use G0 and the controller is disabled. R0 is the source-tool baseline.
R1 adds the random-seek access surface. The access surface must not duplicate a
corpus or reclassify generated artifacts as sources.
